version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - UPLOAD_DIRECTORY=/uploads
    volumes:
      - ./uploads:/uploads
